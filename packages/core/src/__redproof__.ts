// Issue #51 RED PROOF (throwaway). A direct provider endpoint OUTSIDE the gateway — exactly what the #46
// chokepoint forbids. This file exists only to prove CI goes red and blocks the PR; it will be deleted.
export const FORBIDDEN = 'https://api.openai.com/v1/chat/completions';
